/**
 * 运行时版本下解决要使用template模版的方法1:
 * 如何把vue cli创建的项目中切换成完整版本？
 *    我们需要在项目根目录下创建vue.config.js
 */
module.exports = {
  // runtime version + runtimeCompiler => full version
  // runtimeCompiler : true // 启用运行时编译器
}
